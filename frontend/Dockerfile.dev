# Base image
FROM node:20-alpine

# Working directory
WORKDIR /app

# Copy package files
COPY frontend/package*.json ./

# Install dependencies (ci is faster and cleaner for lockfiles)
RUN npm ci

# Copy source code
COPY frontend/ ./

# Expose port
EXPOSE 3000

# Default command for development
CMD ["npm", "run", "dev"]
